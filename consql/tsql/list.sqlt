SELECT * FROM "{{ table.name |i }}" WHERE TRUE
{% if sort -%}
    ORDER BY
        {% for row in sort %}
            "{{ row |i }}" ASC
        {% endfor %}
{%- endif %}
    LIMIT  {{ pager.sql_limit  |i }} OFFSET {{ pager.sql_offset |i }}
